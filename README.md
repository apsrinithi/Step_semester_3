## Date: 15-08-2026

**Today's Work:**
- Covered Java String built-in manipulation methods (`length`, `charAt`, `substring`, `indexOf`, `trim`, `replace`, `split`).
- Practiced ASCII character code conversion and manipulation between characters and ASCII values.
- Implemented mutable string handling with `StringBuilder` and `StringBuffer` and analyzed string performance comparisons.

**Next Session Plan:**
- Implement problem solutions inside `src/main/java/string_operations/class_problems` and `src/main/java/string_operations/assignment_problems` on `feature/session_2`.

**Issues Faced:**
- None

---

## Date: 08-08-2026

**Today's Work:**
- Practice on Java Strings (creation, escape sequences, string arrays, built-in methods, ASCII character codes).
- Practice on Java Exception Handling (hierarchy, checked exceptions, and RuntimeException/unchecked exceptions).

**Next Session Plan:**
- Implement practice and assignment problem solutions inside `src/main/java/string/class_problems` and `src/main/java/string/assignment_problems` on `feature/session_1`.

**Issues Faced:**
- None

---